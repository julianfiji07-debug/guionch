# CLAUDE.md

## Project Overview

**guionch** is a newly initialized repository. It currently contains no source code, build system, or development infrastructure — only a placeholder README.

## Repository Structure

```
/
├── README.md          # Boilerplate project README (placeholder content)
├── CLAUDE.md          # This file — AI assistant guidance
└── .git/              # Git metadata
```

## Current State

- **No source code** — no implementation files in any language
- **No build system** — no package.json, Makefile, Cargo.toml, or similar
- **No dependencies** — no dependency management files
- **No tests** — no test framework or test files
- **No CI/CD** — no workflow or pipeline configuration
- **No linting/formatting** — no eslint, prettier, or equivalent config
- **No .gitignore** — nothing is excluded from version control yet

## Git Details

- **Remote:** `julianfiji07-debug/guionch`
- **Default branch:** `master`
- **Initial commit:** `642a61a` — "Create initial README.md" (2026-02-07)

## Guidelines for AI Assistants

### When Adding New Code

Since this is a blank-slate repository, any new code should establish conventions from the start:

1. **Add a `.gitignore`** appropriate for the chosen language/framework before committing generated artifacts
2. **Add a build/dependency file** (package.json, requirements.txt, Cargo.toml, etc.) as the first development step
3. **Set up linting and formatting** early to enforce consistent style
4. **Add tests** alongside any new functionality
5. **Update this CLAUDE.md** as the project takes shape — document new commands, structure, and conventions

### Commit Practices

- Write clear, descriptive commit messages
- Keep commits focused on a single logical change
- Do not commit secrets, credentials, or environment files

### Updating This File

This CLAUDE.md should be kept in sync with the project as it evolves. When making significant changes (new tooling, new directories, new workflows), update the relevant sections above.
